/*
 * Parte di ripasso;
 * Primo principio della programmazione classica: La sequenza (i comandi vengono eseguiti secondo un certo ordine, dall'alto al basso)
 * Secondo principio della programmazione classica: la selezione (in funzione di determinate condizioni si cambia il flusso del codice, if, else if e switch)
 * 
 * Parte nuova:
 * Terzo principio della programmazione classica: l'iterazione (cicli di operazioni svolti più volte, si parte dal do{...}while(condizione))
 * 
 * Avendo una variabile boolean (true o false) è possibile mettere il "!" prima della variabile per ottenere l'opposto del suo valore.
 * 
 * Per incrementare una variabile di una unità si può scrivere variabile++ ( equivale a: variabile = variabile + 1; )
 */
/*
	Per sistemare meglio le stringhe è possibile utilizzare la funzione stringa.trim() per togliere gli spazi immessi dall'utente.
	È anche possibile concatenare più funzioni. Es. stringa.trim().toLowerCase.
